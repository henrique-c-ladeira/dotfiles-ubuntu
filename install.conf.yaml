- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.vim:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/dev
    - ~/Android/Sdk  

- shell:
    - 
      command: ./setup/node.zsh
      stdout: true
      stderr: true
    - 
      command: ./setup/jdk.zsh
      stdout: true
      stderr: true
    - [sudo apt install bat, Installing bat...]
    - [sudo snap install --classic code, Installing Visual Code Studio...] 
