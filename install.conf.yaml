- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.vim:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/dev  

- shell:
    - 
      command: ./node_setup.zsh
      stdout: true
      stderr: true        
    - [sudo apt install bat, Installing bat...]
    - [sudo snap install --classic code, Installing Visual Code Studio...] 
