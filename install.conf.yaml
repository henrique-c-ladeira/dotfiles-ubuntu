- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.vim:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/dev  

- shell:        
    - [sudo apt install bat, Installing bat...] 
